// app/reset-password/layout.jsx
// This page renders outside the (public) layout group intentionally —
// no Navbar or Footer — just the full-screen auth page.
export default function ResetPasswordLayout({ children }) {
  return <>{children}</>
}
